import Flutter
import UIKit
import Foundation

public class SwiftCryptoPlugin: NSObject, FlutterPlugin {

    public static func register(with registrar: FlutterPluginRegistrar) {
    // DMSOpenPgp contains static library and not support move from flutter plugin
    // you can past implementation in Runner(root)
    }
}
