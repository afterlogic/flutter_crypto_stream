name: example
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  crypto_plugin:
    path: ..
  path_provider: ^1.6.1
dev_dependencies:
  flutter_test:
    sdk: flutter
